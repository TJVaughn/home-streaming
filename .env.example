CAM1_IP=192.168.x.x
CAM2_IP=192.168.x.x
NTFY_URL=https://ntfy.sh/your-topic-here

# Remote access (required only if exposing port 3000 outside your network)
# Generate APP_SECRET with: openssl rand -hex 32
APP_PASSWORD=choose-a-strong-password
APP_SECRET=replace-with-output-of-openssl-rand-hex-32
